# Immich v2.x Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Nasadiť Immich v2.x na smart-home-pc ako Google Photos náhradu — záloha z iPhone + bulk import existujúcej knižnice, bez ML (3.8 GB RAM).

**Architecture:** 3 kontajnery (immich-server, postgres s pgvector, redis/valkey) na default bridge sieti. ML zakomentovaný. Fotky na internom SSD (`./library/`), external read-only bind mount pre bulk import. Port 2283 na LAN.

**Tech Stack:** Docker Compose v2, Immich `release` tag, Valkey 8, pgvector postgres 14, SSH na smart-home-pc

---

### Task 1: Vytvoriť adresárovú štruktúru na serveri

**Files:**
- Create (server): `/opt/stacks/immich/` (adresár)
- Create (server): `/opt/stacks/immich/library/` (bind mount pre upload)
- Create (server): `/opt/stacks/immich/.gitkeep` (aby library/ existoval)

- [ ] **Step 1: Overiť že /opt/stacks existuje**

```bash
ssh smart-home-pc "ls /opt/stacks/"
```

Expected: vypíše existujúce stacks (adguardhome, dockge, ...)

- [ ] **Step 2: Vytvoriť adresáre**

```bash
ssh smart-home-pc "mkdir -p /opt/stacks/immich/library && touch /opt/stacks/immich/library/.gitkeep"
```

Expected: bez chyby

- [ ] **Step 3: Overiť štruktúru**

```bash
ssh smart-home-pc "find /opt/stacks/immich -type f -o -type d | sort"
```

Expected:
```
/opt/stacks/immich
/opt/stacks/immich/library
/opt/stacks/immich/library/.gitkeep
```

---

### Task 2: Vytvoriť compose.yaml

**Files:**
- Create (server): `/opt/stacks/immich/compose.yaml`

- [ ] **Step 1: Zapísať compose.yaml na server**

```bash
ssh smart-home-pc "cat > /opt/stacks/immich/compose.yaml" << 'EOF'
name: immich

services:
  immich-server:
    container_name: immich-server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    volumes:
      - ${UPLOAD_LOCATION}:/usr/src/app/upload
      - ${EXTERNAL_PATH}:/usr/src/app/external:ro
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    ports:
      - "2283:2283"
    depends_on:
      immich-redis:
        condition: service_healthy
      immich-postgres:
        condition: service_healthy
    restart: always
    healthcheck:
      disable: false

  # immich-machine-learning:
  #   container_name: immich-machine-learning
  #   image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}
  #   volumes:
  #     - model-cache:/cache
  #   env_file:
  #     - .env
  #   environment:
  #     MACHINE_LEARNING_WORKERS: 1
  #   restart: always

  immich-redis:
    container_name: immich-redis
    image: docker.io/valkey/valkey:8-bookworm
    healthcheck:
      test: valkey-cli ping || exit 1
    restart: always

  immich-postgres:
    container_name: immich-postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.3.0-pgvecto.rs0.3.0
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: "--data-checksums"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: >-
        pg_isready --dbname="$$POSTGRES_DB" --username="$$POSTGRES_USER" || exit 1;
        Chksum="$$(psql --dbname="$$POSTGRES_DB" --username="$$POSTGRES_USER" --tuples-only --no-align
        --command='SELECT COALESCE(SUM(checksum_failures), 0) FROM pg_stat_database')";
        echo "checksum failure count - $$Chksum";
        [ "$$Chksum" = '0' ] || exit 1
      interval: 5m
      start_period: 5m

volumes:
  postgres-data:
  # model-cache:
EOF
```

Expected: bez chyby

- [ ] **Step 2: Overiť obsah**

```bash
ssh smart-home-pc "docker compose -f /opt/stacks/immich/compose.yaml config --quiet && echo 'compose valid'"
```

Expected: `compose valid`

---

### Task 3: Vytvoriť .env.example a .env

**Files:**
- Create (server): `/opt/stacks/immich/.env.example`
- Create (server): `/opt/stacks/immich/.env`
- Create (local repo): `immich-stack/.env.example` (kópia pre git)

- [ ] **Step 1: Zapísať .env.example na server**

```bash
ssh smart-home-pc "cat > /opt/stacks/immich/.env.example" << 'EOF'
# Database
DB_PASSWORD=change-me
DB_USERNAME=immich
DB_DATABASE_NAME=immich

# Storage
UPLOAD_LOCATION=./library
EXTERNAL_PATH=/home/user/Photos

# Immich
IMMICH_VERSION=release

# Machine Learning (disabled — uncomment when RAM allows)
# MACHINE_LEARNING_WORKERS=1
EOF
```

- [ ] **Step 2: Vygenerovať silné heslo a zapísať .env**

```bash
DB_PASS=$(ssh smart-home-pc "openssl rand -hex 32")
ssh smart-home-pc "cat > /opt/stacks/immich/.env" << EOF
# Database
DB_PASSWORD=${DB_PASS}
DB_USERNAME=immich
DB_DATABASE_NAME=immich

# Storage
UPLOAD_LOCATION=./library
EXTERNAL_PATH=/home/user/Photos

# Immich
IMMICH_VERSION=release
EOF
```

- [ ] **Step 3: Overiť že .env má hodnoty**

```bash
ssh smart-home-pc "grep -c '.' /opt/stacks/immich/.env"
```

Expected: `11` (alebo viac — počet neprázdnych riadkov)

- [ ] **Step 4: Overiť že EXTERNAL_PATH adresár existuje (alebo vytvoriť)**

```bash
ssh smart-home-pc "ls /home/user/Photos 2>/dev/null || (mkdir -p /home/user/Photos && echo 'created Photos dir')"
```

Expected: vypíše obsah alebo `created Photos dir`

---

### Task 4: Spustiť stack a overiť zdravie

**Files:** žiadne nové — len spustenie

- [ ] **Step 1: Stiahnuť images**

```bash
ssh smart-home-pc "cd /opt/stacks/immich && docker compose pull"
```

Expected: `Pull complete` pre immich-server, immich-postgres, immich-redis. Môže trvať 2–5 min (immich-server ~500 MB).

- [ ] **Step 2: Spustiť stack**

```bash
ssh smart-home-pc "cd /opt/stacks/immich && docker compose up -d"
```

Expected:
```
Container immich-postgres  Started
Container immich-redis     Started
Container immich-server    Started
```

- [ ] **Step 3: Počkať na zdravie (30 sekúnd)**

```bash
ssh smart-home-pc "sleep 30 && docker compose -f /opt/stacks/immich/compose.yaml ps"
```

Expected: všetky 3 kontajnery `Up` alebo `healthy`

- [ ] **Step 4: Smoke test — ping API**

```bash
ssh smart-home-pc "curl -sf http://localhost:2283/api/server/ping | grep -q pong && echo 'API OK'"
```

Expected: `API OK`

Ak zlyhá (connection refused): počkaj ďalších 30 sekúnd, zopakuj. Immich-server štartuje ~20–30s po postgres.

- [ ] **Step 5: Overiť logy — žiadne FATAL chyby**

```bash
ssh smart-home-pc "docker logs immich-server --tail 20 2>&1 | grep -i 'error\|fatal' || echo 'no errors'"
```

Expected: `no errors` alebo iba INFO/WARN logy

---

### Task 5: Prvotné nastavenie — admin účet

**Files:** žiadne — konfigurácia cez web UI

- [ ] **Step 1: Otvoriť web UI**

Otvor `http://smart-home-pc:2283` v prehliadači (alebo `http://192.168.0.219:2283`).

Expected: Immich onboarding stránka — formulár na vytvorenie admin účtu.

- [ ] **Step 2: Vytvoriť admin účet**

Vyplň:
- Email: tvoj email (napr. `balaz@hako.sk`)
- Heslo: silné heslo
- Meno: David

Klikni "Create account".

Expected: presmerovanie na dashboard.

- [ ] **Step 3: Skontrolovať Storage Template**

Administration → Settings → Storage Template:
- Ponechaj default alebo nastav na `{{y}}/{{MM}}/{{filename}}`

- [ ] **Step 4: Vypnúť Machine Learning v UI**

Administration → Settings → Machine Learning → vypni "Enabled".

Expected: ML sekcia deaktivovaná (konzistentné s vypnutým kontajnerom).

---

### Task 6: Nakonfigurovať External Library (bulk import)

**Files:** žiadne — konfigurácia cez web UI + CLI

- [ ] **Step 1: Overiť že external mount funguje**

```bash
ssh smart-home-pc "docker exec immich-server ls /usr/src/app/external"
```

Expected: vypíše obsah `/home/user/Photos` (ak prázdny: prázdny výpis, nie chyba).

- [ ] **Step 2: Pridať External Library cez web UI**

Administration → Libraries → Create Library → External Library:
- Owner: David (admin)
- Import Paths: `/usr/src/app/external`
- Klikni "Create"

- [ ] **Step 3: Spustiť scan knižnice**

Administration → Libraries → klikni na novú knižnicu → "Scan Library Files".

Expected: Jobs → Library scan beží. Fotky sa objavia v galérii.

Alternatíva cez CLI (ak preferuješ):
```bash
docker exec immich-server node /usr/src/app/dist/main library scan
```

---

### Task 7: Pridať Immich do Dockge a commitnúť config

**Files:**
- Create (local repo): `immich-stack/` adresár so skopírovanými konfig súbormi pre git

- [ ] **Step 1: Otvoriť Dockge**

`http://smart-home-pc:5001` → Add Stack → zadaj cestu `/opt/stacks/immich`.

Expected: Immich stack viditeľný v Dockge UI.

- [ ] **Step 2: Skopírovať compose.yaml a .env.example do lokálneho repo**

```bash
mkdir -p /home/user/projects/home-assistant/immich-stack
scp smart-home-pc:/opt/stacks/immich/compose.yaml /home/user/projects/home-assistant/immich-stack/
scp smart-home-pc:/opt/stacks/immich/.env.example /home/user/projects/home-assistant/immich-stack/
```

- [ ] **Step 3: Overiť že .env NIE je v repo**

```bash
grep -r "DB_PASSWORD=" /home/user/projects/home-assistant/immich-stack/ && echo 'WARNING: real password in repo!' || echo 'OK — no secrets'
```

Expected: `OK — no secrets`

- [ ] **Step 4: Commitnúť**

```bash
cd /home/user/projects/home-assistant
git add immich-stack/
git commit -m "feat: add Immich v2.x stack — compose + env.example"
```

---

### Task 8: Aktualizovať CLAUDE.md

**Files:**
- Modify (local): `/home/user/projects/home-assistant/CLAUDE.md`

- [ ] **Step 1: Pridať Immich sekciu do CLAUDE.md**

Do tabuľky Stack (Docker Compose) pridaj riadok:

```markdown
| `immich-server` | `ghcr.io/immich-app/immich-server:release` | bridge | 2283 |
```

Do nového bloku pridaj:

```markdown
### Immich
- Config dir: `/opt/stacks/immich/`
- Web UI: `http://smart-home-pc:2283`
- Upload dir: `/opt/stacks/immich/library/` (presun na `/mnt/media/immich/library/` keď príde USB HDD)
- External library: `/home/user/Photos` → `/usr/src/app/external` (read-only mount)
- ML: vypnuté (3.8 GB RAM)
- Upgrade: `pg_dump` → `docker compose pull && up -d`
```

- [ ] **Step 2: Commitnúť**

```bash
cd /home/user/projects/home-assistant
git add CLAUDE.md
git commit -m "docs: pridaj Immich do CLAUDE.md"
```

---

## Post-install checklist

- [ ] `http://smart-home-pc:2283` dostupné na LAN
- [ ] Admin účet vytvorený
- [ ] ML vypnuté v UI
- [ ] External library namountovaná a nascanovaná
- [ ] Dockge zobrazuje Immich stack
- [ ] compose.yaml + .env.example v git repo
- [ ] CLAUDE.md aktualizovaný

## Budúce kroky (mimo tohto plánu)

- **USB HDD:** zmeniť `UPLOAD_LOCATION` v `.env`, `rsync library/`, restart
- **Tailscale Serve:** `sudo tailscale serve --bg --https 8443 http://localhost:2283`
- **ML:** odkomentovať kontajner v compose.yaml po upgrade hardvéru
- **Restic backup:** riešené v Kroku 8 (backup plán)
